package net.javaguides.InventoryManagementSystem.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration class for Swagger/OpenAPI.
 * Springdoc automatically provides API documentation at /swagger-ui.html.
 */
@Configuration
public class SwaggerConfig {
    // Springdoc-OpenAPI requires no additional configuration for basic use.
    // Visit /swagger-ui.html to access the Swagger UI.
}
//the